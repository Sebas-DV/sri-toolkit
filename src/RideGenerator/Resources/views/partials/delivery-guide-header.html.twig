{% set tradeName = company.trade_name|default('') %}
{% set legalName = company.legal_name|default('') %}
{% set headOffice = company.head_office_address|default('') %}
{% set branch = data.establishment_address|default(company.establishment_address|default('')) %}
{% set special = data.special_taxpayer_number|default(company.special_taxpayer_number|default('')) %}
{% set accounting = data.requires_accounting|default(company.requires_accounting|default('NO')) %}
{% set withholdingAgent = data.withholding_agent_resolution|default(company.withholding_agent_resolution|default(company.withholding_agent|default(''))) %}
{% set rimpe = data.rimpe_label|default(company.rimpe_label|default(company.rimpe_regime_taxpayer|default(''))) %}

<table class="delivery-guide-header">
    <tr>
        <td class="delivery-guide-header-left">
            <div class="delivery-guide-logo-box">
                {% if companyLogo %}
                    <img src="{{ companyLogo }}" class="delivery-guide-logo" alt="Logo">
                {% else %}
                    <span class="delivery-guide-no-logo">NO LOGO</span>
                {% endif %}
            </div>

            <div class="delivery-guide-box delivery-guide-company-box">
                <div class="delivery-guide-header-sub">{{ legalName }}</div>

                {% if tradeName and tradeName != legalName %}
                    <div class="delivery-guide-company-title">{{ tradeName }}</div>
                {% endif %}

                <div>
                    <span class="bold">Dirección Matriz:</span>
                    {{ headOffice }}
                </div>
                <div class="delivery-guide-line-gap">
                    <span class="bold">Dirección Sucursal:</span>
                    {{ branch }}
                </div>

                <div class="delivery-guide-company-meta">
                    {% if special %}
                        <div><span class="bold">Contribuyente Especial Nro:</span> {{ special }}</div>
                    {% endif %}

                    <table>
                        <tr>
                            <td><span class="bold">OBLIGADO A LLEVAR CONTABILIDAD:</span></td>
                            <td class="delivery-guide-meta-value bold">{{ accounting }}</td>
                        </tr>
                        {% if withholdingAgent %}
                            <tr>
                                <td><span class="bold">Agente de Retención Resolución No.:</span></td>
                                <td class="delivery-guide-meta-value bold">{{ withholdingAgent }}</td>
                            </tr>
                        {% endif %}
                        {% if rimpe %}
                            <tr>
                                <td><span class="bold">Contribuyente Régimen RIMPE:</span></td>
                                <td class="delivery-guide-meta-value bold">{{ rimpe }}</td>
                            </tr>
                        {% endif %}
                    </table>
                </div>
            </div>
        </td>

        <td class="delivery-guide-header-spacer">&nbsp;</td>

        <td class="delivery-guide-header-right">
            <div class="delivery-guide-box delivery-guide-document-box">
                <div class="delivery-guide-ruc-title">R.U.C.: {{ company.ruc|default('') }}</div>
                <div class="delivery-guide-document-type">{{ documentTitle }}</div>
                <div><span class="bold">No.:</span> {{ documentNumber }}</div>

                <div class="delivery-guide-line-gap"><span class="bold">NÚMERO DE AUTORIZACIÓN:</span></div>
                <div class="delivery-guide-access-key">{{ authorizationNumber|default(accessKey) }}</div>

                <div class="delivery-guide-authorization-meta">
                    <div><span class="bold">FECHA Y HORA DE AUTORIZACIÓN:</span> {{ authorizationDate|default('') }}</div>
                    <div><span class="bold">AMBIENTE:</span> {{ environmentLabel }}</div>
                    <div><span class="bold">EMISIÓN:</span> {{ emissionLabel }}</div>
                </div>

                <div class="delivery-guide-barcode-box">
                    <div class="bold delivery-guide-barcode-title">CLAVE DE ACCESO</div>
                    <img src="{{ accessKeyBarcode }}" class="delivery-guide-barcode" alt="Código de barras">
                    <div class="delivery-guide-access-key delivery-guide-barcode-key">{{ accessKey }}</div>
                </div>
            </div>
        </td>
    </tr>
</table>
