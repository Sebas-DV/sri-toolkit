{% set tradeName = company.trade_name|default('') %}
{% set legalName = company.legal_name|default('') %}
{% set headOffice = company.head_office_address|default('') %}
{% set branch = data.establishment_address|default(company.establishment_address|default('')) %}
{% set special = data.special_taxpayer_number|default(company.special_taxpayer_number|default('')) %}
{% set accounting = data.requires_accounting|default(company.requires_accounting|default('NO')) %}
{% set withholdingAgent = data.withholding_agent_resolution|default(company.withholding_agent_resolution|default(company.withholding_agent|default(''))) %}
{% set rimpe = data.rimpe_label|default(company.rimpe_label|default(company.rimpe_regime_taxpayer|default(''))) %}

<table class="debit-note-header">
    <tr>
        <td class="debit-note-header-left">
            <div class="debit-note-logo-box">
                {% if companyLogo %}
                    <img src="{{ companyLogo }}" class="debit-note-logo" alt="Logo">
                {% else %}
                    <span class="debit-note-no-logo">NO LOGO</span>
                {% endif %}
            </div>

            <div class="debit-note-box debit-note-company-box">
                <div class="debit-note-header-sub">{{ legalName }}</div>

                {% if tradeName and tradeName != legalName %}
                    <div class="debit-note-company-title">{{ tradeName }}</div>
                {% endif %}

                <div>
                    <span class="bold">Dirección Matriz:</span>
                    {{ headOffice }}
                </div>
                <div class="debit-note-line-gap">
                    <span class="bold">Dirección Sucursal:</span>
                    {{ branch }}
                </div>

                <div class="debit-note-company-meta">
                    {% if special %}
                        <div><span class="bold">Contribuyente Especial Nro:</span> {{ special }}</div>
                    {% endif %}

                    <table>
                        <tr>
                            <td><span class="bold">OBLIGADO A LLEVAR CONTABILIDAD:</span></td>
                            <td class="debit-note-meta-value bold">{{ accounting }}</td>
                        </tr>
                        {% if withholdingAgent %}
                            <tr>
                                <td><span class="bold">Agente Retención Resolución No.:</span></td>
                                <td class="debit-note-meta-value bold">{{ withholdingAgent }}</td>
                            </tr>
                        {% endif %}
                        {% if rimpe %}
                            <tr>
                                <td><span class="bold">Contribuyente Régimen RIMPE:</span></td>
                                <td class="debit-note-meta-value bold">{{ rimpe }}</td>
                            </tr>
                        {% endif %}
                    </table>
                </div>
            </div>
        </td>

        <td class="debit-note-header-spacer">&nbsp;</td>

        <td class="debit-note-header-right">
            <div class="debit-note-box debit-note-document-box">
                <div class="debit-note-ruc-title">R.U.C.: {{ company.ruc|default('') }}</div>
                <div class="debit-note-document-type">{{ documentTitle }}</div>
                <div><span class="bold">No.:</span> {{ documentNumber }}</div>

                <div class="debit-note-line-gap"><span class="bold">NÚMERO DE AUTORIZACIÓN:</span></div>
                <div class="debit-note-access-key">{{ authorizationNumber|default(accessKey) }}</div>

                <div class="debit-note-authorization-meta">
                    <div><span class="bold">FECHA Y HORA DE AUTORIZACIÓN:</span> {{ authorizationDate|default('') }}</div>
                    <div><span class="bold">AMBIENTE:</span> {{ environmentLabel }}</div>
                    <div><span class="bold">EMISIÓN:</span> {{ emissionLabel }}</div>
                </div>

                <div class="debit-note-barcode-box">
                    <div class="bold debit-note-barcode-title">CLAVE DE ACCESO</div>
                    <img src="{{ accessKeyBarcode }}" class="debit-note-barcode" alt="Código de barras">
                    <div class="debit-note-access-key debit-note-barcode-key">{{ accessKey }}</div>
                </div>
            </div>
        </td>
    </tr>
</table>
